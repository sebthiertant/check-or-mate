[project]
name = "ingestion"
version = "0.1.0"
description = "Chess.com game ingestion pipeline"
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.27.0",
  "python-chess>=1.999",
  "pydantic>=2.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ingestion"]
