# check-or-mate — watchlist configuration
#
# strategy: leaderboard → `ingest populate` fetches players from the
#   Chess.com leaderboard before each sync.
# strategy: static      → use the `players` list as-is (no API call).
#
# top_n        : max players to include from the leaderboard (≤ 50 per
#                category × number of categories; duplicates are removed).
# time_class   : bullet | blitz | rapid | daily | all
# extra_players: always included, even if not in the leaderboard.

strategy: leaderboard
top_n: 200
time_class: all

extra_players:
  - polish_fighter3000

# ── Auto-generated by `ingest populate` — do not edit below this line ────────
players:
  - hikaru
  - magnuscarlsen
  - firouzja2003
  - lachesisq
  - polish_fighter3000
